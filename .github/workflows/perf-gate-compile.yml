name: Renderer perf-gate compile check

on:
  push:
    branches: [main]
    paths:
      - "omniphony-renderer/renderer/**"
      - "omniphony-renderer/Cargo.toml"
      - "omniphony-renderer/Cargo.lock"
      - ".github/workflows/perf-gate-compile.yml"
  workflow_dispatch:

permissions:
  contents: read

env:
  CARGO_TERM_COLOR: always
  RUST_TOOLCHAIN: 1.88.0
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: "true"

jobs:
  perf-gate-compile:
    name: Compile release perf gate
    runs-on: ubuntu-latest
    timeout-minutes: 15
    defaults:
      run:
        working-directory: omniphony-renderer

    steps:
      - name: Checkout repository
        uses: actions/checkout@v7

      - name: Install pinned Rust toolchain
        shell: bash
        run: |
          rustup toolchain install "$RUST_TOOLCHAIN" --profile minimal
          rustup override set "$RUST_TOOLCHAIN"
          rustc --version --verbose
          cargo --version

      - name: Compile release-only perf gate
        run: cargo check --release -p renderer --features perf-gate --tests
